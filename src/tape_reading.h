#ifndef TAPE_READING_H_
#define TAPE_READING_H_

typedef struct {
    float price;
    float volume_buy;
    float volume_sell;
    float avg_volume_buy;
    float avg_volume_sell;
} Tape_Reading;

#endif // TAPE_READING_H_
